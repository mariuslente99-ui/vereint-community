FIX: Event-JSON wird jetzt bei jedem Browser-Refresh frisch geladen.

Lokal testen:
1. Ordner im Terminal öffnen.
2. npm start
3. Browser: http://localhost:4173
4. event.json ändern und Browser neu laden.

Nicht per Doppelklick/file:// öffnen. Safari/Chrome können JSON dort blockieren oder cachen. Der kleine lokale Server sendet No-Cache-Header, deshalb passen Anzeige und event.json dauerhaft zusammen.

Das große aftermovie.mp4 ist nicht enthalten. Bitte bei Bedarf wieder neben index.html legen.
